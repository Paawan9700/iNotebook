import React, {useContext} from 'react'
import notecontext from '../context/notes/notecontext'

export const About = () => {
    return (
        <div>
            this is about
        </div>
    )
}
